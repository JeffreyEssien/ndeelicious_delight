import Link from "next/link";import { Icon } from "@/components/ui/icons";export default async function Page({searchParams}:{searchParams:Promise<{order?:string}>}){const id=(await searchParams).order??"";return <section className="success-page"><span className="success-mark"><Icon name="check"/></span><span className="overline">Order received</span><h1>Your order is looking lovely.</h1><p>Order <b>#{id}</b> has been saved. It remains pending until payment is configured and confirmed.</p><div className="next-steps"><h2>What happens next?</h2><div><span>01</span><p><b>Payment confirmation</b><small>A receipt will be sent by email.</small></p></div><div><span>02</span><p><b>We begin preparing</b><small>The bakery confirms your fulfilment window.</small></p></div><div><span>03</span><p><b>Follow every update</b><small>Track progress with your order number.</small></p></div></div><div className="success-actions"><Link href={`/track-order?order=${id}`} className="button button-primary">Track this order</Link><Link href="/shop" className="button button-secondary">Continue shopping</Link></div></section>}
