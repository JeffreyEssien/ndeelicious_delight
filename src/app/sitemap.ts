import type { MetadataRoute } from "next";import { getProducts } from "@/lib/data/catalog";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const products=await getProducts();const base="http://localhost:3000";return ["","/shop","/custom-cakes","/ready-to-bake","/about","/contact","/faq","/delivery-information",...products.map(p=>`/product/${p.slug}`)].map(url=>({url:`${base}${url}`,lastModified:new Date()}))}
